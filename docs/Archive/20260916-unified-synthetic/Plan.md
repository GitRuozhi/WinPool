# WinPool 阶段计划

## 已完成：统一层模拟池、模拟层与产品设计

状态：已实施并验证。日期：2026-09-16。产品版本保持 V0.53。

本节实施已完成，当前无激活任务。下文监控数据库轮换、7z 归档计划仍为未激活，原文保留，等待以后执行；不归档，不因本轮收口而删除本文件。

产品设计见 [统一对象与派生属性](../../UnifiedModel.md)。固定决定：

- 统一投影生成模拟层，类型为模拟层，名称为热备层、退役层、未划层；归属未知成员进入未划层并另标明未知。
- 现有其它、网络分组成为其它磁盘池、网络磁盘池，类型为模拟池。
- 假池/假层共用尽量一致的真实池/层属性结构；名称、类型、成员数量正常显示，无来源参数留空，原始字段显示无原始来源。
- 假池/假层不计算容量、不污染来源事实或持久化。实际层使用自身容量，禁止成员容量合计回填层容量。
- 管理页中的假池/假层可选，列表、拓扑、属性和导航一致；原有实际设备计数、模拟系统编辑和真实写入拒绝边界保持。

分工：Terra-Max 子代理负责 src/tests 代码与相关回归；主代理负责文档、代码审查和最终验证。不推送，不执行真实存储结构变更。

| 任务 | 状态 |
| --- | --- |
| 统一对象专项产品设计与文档入口 | 已完成，链接核对通过 |
| 统一模型、管理投影、来源详情及显示接线 | 已实现；有限原生页面核对通过 |
| 假对象隔离、容量与选择等直接行为回归 | 已验证 |
| 主代理审查、Release 自动测试与完整构建 | 已完成；613/613，构建 0 警告/0 错误 |

验收按 UnifiedModel 的验证要求执行，至少覆盖真实层容量不等于成员合计、假层/假池无容量且无来源、未知归属、孤立联合体、刷新和选择、来源序列化不被污染及计数不重复。最终运行全套 Release 测试与完整构建，构建前检查运行树占用并使用独立输出。原生 UI 未运行时明确记录 unverified；不得以自动门代替视觉和真实设备证据。

实际结果和证据路径见 [Quality](../../Quality.md)。已核对中文三个模拟层、两个模拟池和英文未划层的选择、类型、空容量及无原始来源；未执行完整 DPI/主题/拖放矩阵和真实存储写入。其它任务同期归档设计文档导致的旧架构路径断言已单独同步，未回退其文档改动。
