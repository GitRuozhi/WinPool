# 2026-09-16 现行文档整理

本次仅整理文档，不执行代码测试、构建或原生验证，不改变历史通过或未验证状态。

[Quality 整理前全文](Quality-before.md)保留当时各阶段的验证记录、命令、产物路径和限制。原文来自 docs/Quality.md，其相对链接按原 docs 目录解释；当前规则见[Quality](../../Quality.md)。613 项结果属于统一对象阶段，603/598 项属于此前采集修复，不能混作同一基线验证。

## 从现行规范移出的历史说明

Product 原段落：

2026-09-11 新决定已实施：已有对象在名称输入框按回车单独提交改名、即时生效（不增加保存名称按钮，普通失焦不提交），不参与结构动作或结构撤销/放弃；名称仅用于显示，身份和关联使用可靠 ID。尚未创建对象的名称属于创建参数。选择切换、删除覆盖属性意图、独立卷标、意图撤销和解散原因聚合已修复，但本轮按用户限制未运行验证，不能据此宣称全部验收通过。

Development 原段落：

此轮按用户要求只构建和人工检查，不运行自动测试。原生 UI 验收使用独立运行目录和数据库副本；固定采集脚本始终内嵌，经标准输入执行，不输出独立 inventory 脚本。

上述段落的“本轮”“此轮”指各自历史任务，不适用于当前或未来任务。有效行为保留在 Product/Development，既有历史结果仍见 [CHANGELOG](../../CHANGELOG.md)。
