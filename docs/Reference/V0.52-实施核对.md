# V0.52 实施核对

本文件只记录活动 Plan 的基线与核对证据，不是第二份执行计划或永久字段规范。

## 基线

- 实现基线 `85a3371`，SDK 10.0.400，当前产品 V0.51。2026-09-14 接手逐项目 Release 测试 510/510 passed。
- 开始时无 WinPool App/Agent 进程。已有用户数据不转换、不移动，后续程序验证使用独立新数据根。
- 同机固定只读完整采集：10,169 ms；脱敏和 JSON 序列化：57 ms；JSON 70,197 bytes；3 块物理盘、8 个分区、154 个旧报告项目。临时探针只输出计时/数量，不保存原始采集。首屏与新旧刷新比较仍 unverified。

## 迁移核对入口

| 当前入口 | 有效内容和退出要求 | 状态 |
| --- | --- | --- |
| RawSnapshot / EmbeddedStorageInventoryScript | 保留各来源原值；DeviceId 字符串、状态数组、分区类型与卷容量分别贯通；区分失败/null | 待实施 |
| RawHardware / HardwareReportFactory / KsReferenceReportFactory | 原 13 类154项的有效信息按对象及来源展示，退出平行数组和独立当前存储报告 | 待实施 |
| StorageSnapshot / StorageSystemDocument / InventorySnapshot | 事实、统一投影、应用身份分工；共同存储字段只解释一次 | 实施中 |
| AgentInventoryCoordinator / InventoryPipeline | 存储与硬件刷新拆开；保留串行门、取消与错误路由；旧结果不覆盖新事实 | 待实施 |
| StorageSystemRepository / SQLite / IPC | 新格式、身份、原子保存与CommitId；退出旧格式缓存回退；检查实际序列化帧体积 | 待实施 |
| StorageStructurePage / SimulationDraftPlanner / SimulationEditCoordinator | 基线、MAX、创建意图与历史迁移到编辑会话，复用规则和操作，不改变交互 | 待实施 |
| SimulatedCommandText / 动作和结果详情 | 同一计划派生有限命令；替换伪命令/未转义参数，保留no-op和未绑定说明 | 待实施 |
| MainWindow / 管理页 / 硬件页 / 导出 / 监控 | 统一消费事实投影及来源详情，必要接线；原布局/监控算法保留 | 待实施 |

## 风险覆盖

精简 Plan 的六类验证分别覆盖审查稿 D01–D08（字段/实体/来源）、P01–P06（保存/复制/刷新）、E01–E06（编辑序列）、C01–C03（命令）、A01–A03（安全/旧路径/既有功能）。原生检查 N01–N06 按活动 Plan 的有限实测执行。编号只用于追踪覆盖，实际结果随实现记录，不能用旧版通过记录替代。

## 当前验证

- Application 回归：219/219 passed。覆盖 UInt64 极值、返回null/false/数组、查询失败、身份重建、复制、迟到/失败采集与无依据分组；新增复制后存储引用与事实身份一致、修改不影响原系统。
- Infrastructure 回归：46/46 passed，包括本机固定只读存储/完整硬件采集、成功空集合删除缓存对象、失败保留事实、硬件可靠来源身份及严格缓存哈希校验。旧缓存忽略哈希失败的断言已退出；原生卷现在断言独立 Volume 类型。
- 分用途探针：存储采集 2,593 / 3,346 ms，完整硬件 10,307 ms；脱敏 JSON 约 243 KB / 556 KB。仍含迁移中的旧报告和快照，不是最终体积/性能结论。
- 隔离 Release 构建 passed，0 warning / 0 error。原生导航测试发现纯代码 HardwarePage 进入时 App 崩溃，补 XAML 页面注册后，`--page Hardware` 启动及设备字段展示 passed；通过原生 ExpandCollapsePattern 展开容量，读到原值 `3000592982016 bytes`、模拟来源和采集时间；模拟刷新按钮禁用、序列号脱敏。这不是完整原生验收。运行目录 `artifacts/V052-review-*`，独立便携 Data；启动前确认全局 storage-location.json 不存在，运行后仍未创建。未修改用户已有数据。
- 当前不代表 V0.52 完成：事实与旧快照/报告仍有重复链路，编辑会话/命令预览及最终格式、原生事件和重启验收尚未收口。
