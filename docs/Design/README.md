# 设计储备

这里保存领先于开发、尚未进入版本计划的方案。设计领先不是过时或无效的证据；实施前仍可由开发者与 Agent 修改。目录中的文档不自动成为当前要求，不默认读取，不据此开始写代码。

每份设计只需明确状态、适用基线/条件和未决问题。状态可为“讨论中”“方向已确认、未排期”“已纳入指定 Plan”；只有被当前 Plan 明确采纳的部分进入实施范围。详细流程归 [Development](../Development.md)。

| 方案 | 当前定位 | 重新排期前核对 |
| --- | --- | --- |
| [硬件读取分析与重构方案](WinPool-Hardware-Inventory-Analysis-and-Refactor-Plan.md) | 设计储备；源方案基于 V0.45，未纳入 V0.48 | 硬件页是否进入产品范围、StorageFast/HardwareFull 是否仍必要、V0.48 后实际模型及采集边界 |
| [多版本发行方案](WinPool-Multi-Edition-Plan-Simplified.md) | 设计储备；源方案基于 V0.45 之后，未纳入 V0.48 | Standard/Portable/Preview 的必要性、功能范围、MSIX 阶段、届时的数据策略和平台验证 |

正文保留当时的分析与建议，包括“必须”“最终”等措辞；这些措辞不是实施授权。当前 Product/Development 和用户新决定优先。V0.48 无数据兼容要求，不能从多发行方案反向引入兼容层；1.x 的测试/开发占位也不因该方案的功能表而改变。

纳入未来版本时只选择届时需要的部分，修正过期事实，记录尚未解决的选择，再进入唯一活动 `docs/Plan.md`；长期规则回填所属文档。被替代的设计才移入 Archive，不因暂未实施就归档丢失其讨论价值。
