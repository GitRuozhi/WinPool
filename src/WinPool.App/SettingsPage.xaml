<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="WinPool_App.SettingsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Page.Resources>
        <Style x:Key="SettingsCardBorder" TargetType="Border">
            <Setter Property="Padding" Value="20" />
            <Setter Property="Background" Value="{ThemeResource CardBackgroundFillColorDefaultBrush}" />
            <Setter Property="BorderBrush" Value="{ThemeResource CardStrokeColorDefaultBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="CornerRadius" Value="4" />
        </Style>
        <Style x:Key="SettingsSectionTitle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="18" />
            <Setter Property="FontWeight" Value="SemiBold" />
        </Style>
        <Style x:Key="SettingsFieldLabel" TargetType="TextBlock">
            <Setter Property="FontSize" Value="16" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="VerticalAlignment" Value="Center" />
        </Style>
        <Style x:Key="SettingsFieldValue" TargetType="TextBlock">
            <Setter Property="VerticalAlignment" Value="Center" />
        </Style>
    </Page.Resources>

    <ScrollViewer HorizontalContentAlignment="Stretch">
        <StackPanel
            Padding="24,20,24,40"
            HorizontalAlignment="Stretch"
            Spacing="16">
            <Border Style="{StaticResource SettingsCardBorder}">
                <Grid ColumnSpacing="16" RowSpacing="16">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="220" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <TextBlock
                        x:Name="AppearanceSectionTitle"
                        Grid.ColumnSpan="2"
                        Style="{StaticResource SettingsSectionTitle}"
                        Text="外观" />
                    <TextBlock
                        x:Name="ThemeTitle"
                        Grid.Row="1"
                        Style="{StaticResource SettingsFieldLabel}"
                        Text="主题" />
                    <ComboBox
                        x:Name="ThemeOptions"
                        Grid.Row="1"
                        Grid.Column="1"
                        MinWidth="220"
                        HorizontalAlignment="Left"
                        VerticalAlignment="Center"
                        SelectionChanged="ThemeOptions_SelectionChanged" />
                    <TextBlock
                        x:Name="AccentTitle"
                        Grid.Row="2"
                        Style="{StaticResource SettingsFieldLabel}"
                        Text="主题色" />
                    <ComboBox
                        x:Name="AccentOptions"
                        Grid.Row="2"
                        Grid.Column="1"
                        MinWidth="220"
                        HorizontalAlignment="Left"
                        VerticalAlignment="Center"
                        SelectionChanged="AccentOptions_SelectionChanged" />
                    <TextBlock
                        x:Name="LanguageTitle"
                        Grid.Row="3"
                        Style="{StaticResource SettingsFieldLabel}"
                        Text="语言" />
                    <ComboBox
                        x:Name="LanguageOptions"
                        Grid.Row="3"
                        Grid.Column="1"
                        MinWidth="220"
                        HorizontalAlignment="Left"
                        VerticalAlignment="Center"
                        SelectionChanged="LanguageOptions_SelectionChanged" />
                </Grid>
            </Border>

            <Border Style="{StaticResource SettingsCardBorder}">
                <Grid ColumnSpacing="16" RowSpacing="16">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="220" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <TextBlock
                        x:Name="GeneralSectionTitle"
                        Grid.ColumnSpan="2"
                        Style="{StaticResource SettingsSectionTitle}"
                        Text="常规" />
                    <TextBlock
                        x:Name="ExecutionTitle"
                        Grid.Row="1"
                        Style="{StaticResource SettingsFieldLabel}"
                        Text="执行模式" />
                    <CheckBox
                        x:Name="SettingsExecutionModeSwitch"
                        Grid.Row="1"
                        Grid.Column="1"
                        HorizontalAlignment="Left"
                        VerticalAlignment="Center"
                        Click="SettingsExecutionModeSwitch_Click"
                        Content="本机真实操作" />
                    <TextBlock
                        x:Name="MsrTitle"
                        Grid.Row="2"
                        Style="{StaticResource SettingsFieldLabel}"
                        Text="初始化磁盘" />
                    <CheckBox
                        x:Name="MsrCheckBox"
                        Grid.Row="2"
                        Grid.Column="1"
                        HorizontalAlignment="Left"
                        VerticalAlignment="Center"
                        Click="MsrCheckBox_Click"
                        Content="初始化磁盘时创建微软保留分区（MSR）" />
                    <TextBlock
                        x:Name="PrivacyTitle"
                        Grid.Row="3"
                        Style="{StaticResource SettingsFieldLabel}"
                        Text="隐私" />
                    <CheckBox
                        x:Name="ShowHardwareIdsCheckBox"
                        Grid.Row="3"
                        Grid.Column="1"
                        HorizontalAlignment="Left"
                        VerticalAlignment="Center"
                        Click="ShowHardwareIdsCheckBox_Click"
                        Content="显示硬件ID" />
                    <TextBlock
                        x:Name="WelcomeTitle"
                        Grid.Row="4"
                        Style="{StaticResource SettingsFieldLabel}"
                        Text="欢迎页面" />
                    <Button
                        x:Name="WelcomeButton"
                        Grid.Row="4"
                        Grid.Column="1"
                        HorizontalAlignment="Left"
                        VerticalAlignment="Center"
                        Click="WelcomeButton_Click"
                        Content="打开欢迎页面" />
                    <TextBlock
                        x:Name="StartupAgentTitle"
                        Grid.Row="5"
                        Style="{StaticResource SettingsFieldLabel}"
                        Text="登录启动" />
                    <CheckBox
                        x:Name="StartupAgentCheckBox"
                        Grid.Row="5"
                        Grid.Column="1"
                        HorizontalAlignment="Left"
                        VerticalAlignment="Center"
                        Click="StartupAgentCheckBox_Click"
                        Content="登录 Windows 时启动托盘 Agent" />
                    <TextBlock
                        x:Name="DataLocationTitle"
                        Grid.Row="6"
                        Style="{StaticResource SettingsFieldLabel}"
                        Text="数据存储位置" />
                    <Grid
                        Grid.Row="6"
                        Grid.Column="1"
                        ColumnSpacing="12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <ComboBox
                            x:Name="DataLocationOptions"
                            MinWidth="220"
                            HorizontalAlignment="Left"
                            VerticalAlignment="Center"
                            SelectionChanged="DataLocationOptions_SelectionChanged" />
                        <TextBlock
                            x:Name="DataLocationPath"
                            Grid.Column="1"
                            VerticalAlignment="Center"
                            FontSize="12"
                            Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                            TextTrimming="CharacterEllipsis"
                            ToolTipService.ToolTip="{Binding Text, RelativeSource={RelativeSource Self}}" />
                    </Grid>
                </Grid>
            </Border>

            <Border Style="{StaticResource SettingsCardBorder}">
                <Grid ColumnSpacing="16" RowSpacing="16">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="220" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <TextBlock
                        x:Name="AboutSectionTitle"
                        Grid.ColumnSpan="2"
                        Style="{StaticResource SettingsSectionTitle}"
                        Text="关于" />
                    <TextBlock
                        x:Name="AboutProductNameLabel"
                        Grid.Row="1"
                        Style="{StaticResource SettingsFieldLabel}"
                        Text="产品" />
                    <TextBlock
                        x:Name="AboutProductNameValue"
                        Grid.Row="1"
                        Grid.Column="1"
                        Style="{StaticResource SettingsFieldValue}"
                        Text="WinPool" />
                    <TextBlock
                        x:Name="AboutVersionLabel"
                        Grid.Row="2"
                        Style="{StaticResource SettingsFieldLabel}"
                        Text="版本" />
                    <TextBlock
                        x:Name="AboutVersionValue"
                        Grid.Row="2"
                        Grid.Column="1"
                        Style="{StaticResource SettingsFieldValue}" />
                    <TextBlock
                        x:Name="AboutProviderLabel"
                        Grid.Row="3"
                        Style="{StaticResource SettingsFieldLabel}"
                        Text="提供方" />
                    <TextBlock
                        Grid.Row="3"
                        Grid.Column="1"
                        Style="{StaticResource SettingsFieldValue}"
                        Text="GitRuozhi" />
                    <TextBlock
                        x:Name="AboutWebsiteLabel"
                        Grid.Row="4"
                        Style="{StaticResource SettingsFieldLabel}"
                        Text="官网" />
                    <Button
                        x:Name="WebsiteButton"
                        Grid.Row="4"
                        Grid.Column="1"
                        HorizontalAlignment="Left"
                        VerticalAlignment="Center"
                        Click="WebsiteLink_Click">
                        <StackPanel Orientation="Horizontal" Spacing="6">
                            <FontIcon FontSize="13" Glyph="&#xE774;" />
                            <TextBlock x:Name="WebsiteButtonText" VerticalAlignment="Center" Text="访问官网" />
                        </StackPanel>
                    </Button>
                    <TextBlock
                        x:Name="AboutUpdateLabel"
                        Grid.Row="5"
                        Style="{StaticResource SettingsFieldLabel}"
                        Text="更新" />
                    <Button
                        x:Name="UpdateButton"
                        Grid.Row="5"
                        Grid.Column="1"
                        HorizontalAlignment="Left"
                        VerticalAlignment="Center"
                        Click="UpdateLink_Click">
                        <StackPanel Orientation="Horizontal" Spacing="6">
                            <FontIcon FontSize="13" Glyph="&#xE777;" />
                            <TextBlock x:Name="UpdateButtonText" VerticalAlignment="Center" Text="查看更新" />
                        </StackPanel>
                    </Button>
                    <TextBlock
                        x:Name="AboutFeedbackLabel"
                        Grid.Row="6"
                        Style="{StaticResource SettingsFieldLabel}"
                        Text="反馈" />
                    <Button
                        x:Name="FeedbackButton"
                        Grid.Row="6"
                        Grid.Column="1"
                        HorizontalAlignment="Left"
                        VerticalAlignment="Center"
                        Click="FeedbackLink_Click">
                        <StackPanel Orientation="Horizontal" Spacing="6">
                            <FontIcon FontSize="13" Glyph="&#xE8F2;" />
                            <TextBlock x:Name="FeedbackButtonText" VerticalAlignment="Center" Text="提交反馈" />
                        </StackPanel>
                    </Button>
                    <TextBlock
                        x:Name="AboutCommunityLabel"
                        Grid.Row="7"
                        Style="{StaticResource SettingsFieldLabel}"
                        Text="轻松交流" />
                    <Button
                        x:Name="AboutCommunityButton"
                        Grid.Row="7"
                        Grid.Column="1"
                        HorizontalAlignment="Left"
                        VerticalAlignment="Center"
                        Click="CommunityButton_Click">
                        <StackPanel Orientation="Horizontal" Spacing="6">
                            <FontIcon FontSize="13" Glyph="&#xE8F2;" />
                            <TextBlock
                                x:Name="CommunityButtonText"
                                VerticalAlignment="Center"
                                Text="加入 QQ 群" />
                        </StackPanel>
                    </Button>
                </Grid>
            </Border>
        </StackPanel>
    </ScrollViewer>
</Page>
