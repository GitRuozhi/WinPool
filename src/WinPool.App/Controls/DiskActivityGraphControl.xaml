<?xml version="1.0" encoding="utf-8" ?>
<UserControl
    x:Class="WinPool_App.Controls.DiskActivityGraphControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    <Grid>
        <Border x:Name="ProbeGridline" Width="0" Height="0" Background="{ThemeResource ControlStrokeColorDefaultBrush}" Visibility="Collapsed" />
        <Canvas x:Name="PlotCanvas" SizeChanged="PlotCanvas_SizeChanged" />
        <TextBlock
            x:Name="ActivityMaxLabel"
            Margin="6,4,0,0"
            HorizontalAlignment="Left"
            VerticalAlignment="Top"
            FontSize="11"
            Foreground="{ThemeResource TextFillColorSecondaryBrush}"
            Text="100%" />
        <TextBlock
            x:Name="SpeedMaxLabel"
            Margin="0,4,6,0"
            HorizontalAlignment="Right"
            VerticalAlignment="Top"
            FontSize="11"
            Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
    </Grid>
</UserControl>
