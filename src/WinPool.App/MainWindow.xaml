<?xml version="1.0" encoding="utf-8" ?>
<Window
    x:Class="WinPool_App.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:controls="using:WinPool_App.Controls"
    xmlns:local="using:WinPool_App"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Title="WinPool"
    mc:Ignorable="d">
    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid x:Name="RootGrid">
        <Grid.RowDefinitions>
            <RowDefinition Height="48" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid
            x:Name="CustomTitleBar"
            Grid.Row="0"
            Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
            BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
            BorderThickness="0,0,0,1"
            Loaded="CustomTitleBar_Loaded"
            SizeChanged="CustomTitleBar_SizeChanged">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <StackPanel
                Grid.Column="0"
                Margin="14,0,20,0"
                VerticalAlignment="Center"
                Orientation="Horizontal"
                Spacing="10">
                <Image Width="22" Height="22" Source="Assets/CAppIcon.ico" />
                <TextBlock
                    x:Name="WindowTitleText"
                    VerticalAlignment="Center"
                    FontSize="15"
                    FontWeight="SemiBold"
                    Text="WinPool" />
            </StackPanel>

            <ListView
                x:Name="ShellNavigationList"
                Grid.Column="1"
                VerticalAlignment="Center"
                ItemsSource="{x:Bind ShellNavigationItems}"
                SelectedItem="{x:Bind SelectedShellItem, Mode=TwoWay}"
                SelectionChanged="ShellNavigationList_SelectionChanged"
                SelectionMode="Single"
                ScrollViewer.HorizontalScrollBarVisibility="Hidden"
                ScrollViewer.HorizontalScrollMode="Disabled">
                <ListView.ItemContainerStyle>
                    <Style TargetType="ListViewItem">
                        <Setter Property="MinWidth" Value="0" />
                        <Setter Property="Margin" Value="0" />
                        <Setter Property="Padding" Value="0" />
                        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                        <Setter Property="VerticalContentAlignment" Value="Stretch" />
                    </Style>
                </ListView.ItemContainerStyle>
                <ListView.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel Orientation="Horizontal" />
                    </ItemsPanelTemplate>
                </ListView.ItemsPanel>
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <Grid
                            Width="{Binding ItemWidth}"
                            MinWidth="42"
                            Padding="10,0"
                            HorizontalAlignment="Center"
                            Background="{Binding Background}"
                            ToolTipService.ToolTip="{Binding Title}">
                            <StackPanel
                                HorizontalAlignment="Center"
                                Orientation="Horizontal"
                                Spacing="6">
                                <FontIcon
                                    FontSize="14"
                                    Foreground="{Binding Foreground}"
                                    Glyph="{Binding Glyph}" />
                                <TextBlock
                                    VerticalAlignment="Center"
                                    Foreground="{Binding Foreground}"
                                    Text="{Binding Title}"
                                    Visibility="{Binding TextVisibility}" />
                            </StackPanel>
                        </Grid>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>

            <Border
                x:Name="ActiveSystemSelectorHost"
                Grid.Column="2"
                Margin="24,0,12,0"
                MinWidth="180"
                MaxWidth="420"
                HorizontalAlignment="Right"
                VerticalAlignment="Center"
                Background="Transparent"
                BorderThickness="1"
                CornerRadius="4"
                PointerEntered="ActiveSystemSelectorHost_PointerEntered"
                PointerExited="ActiveSystemSelectorHost_PointerExited">
                <ComboBox
                    x:Name="ActiveSystemSelector"
                    Width="320"
                    MinWidth="178"
                    Background="Transparent"
                    BorderThickness="0"
                    HorizontalContentAlignment="Left"
                    AutomationProperties.Name="Storage system"
                    DropDownClosed="ActiveSystemSelector_DropDownClosed"
                    SelectionChanged="ActiveSystemSelector_SelectionChanged"
                    Visibility="Collapsed">
                    <ComboBox.ItemContainerStyle>
                        <Style TargetType="ComboBoxItem">
                            <Setter Property="HorizontalContentAlignment" Value="Left" />
                        </Style>
                    </ComboBox.ItemContainerStyle>
                </ComboBox>
            </Border>

            <StackPanel
                x:Name="ModeControls"
                Grid.Column="3"
                VerticalAlignment="Center"
                Orientation="Horizontal"
                Spacing="8">
                <TextBlock
                    x:Name="LocalRealOperationsLabel"
                    VerticalAlignment="Center"
                    FontSize="13"
                    Text="真实编辑" />
                <ToggleSwitch
                    x:Name="LocalRealOperationsSwitch"
                    MinWidth="0"
                    VerticalAlignment="Center"
                    OffContent=""
                    OnContent=""
                    Toggled="LocalRealOperationsSwitch_Toggled"
                    AutomationProperties.Name="Real editing" />
            </StackPanel>
        </Grid>

        <Grid Grid.Row="1">
            <Frame x:Name="RootFrame" />

            <InfoBar
                x:Name="LocalRealOperationsWarning"
                Margin="12,6,12,0"
                HorizontalAlignment="Stretch"
                VerticalAlignment="Top"
                Canvas.ZIndex="110"
                IsClosable="True"
                IsOpen="False"
                Severity="Warning"
                CloseButtonClick="LocalRealOperationsWarning_CloseButtonClick"
                Title="测试版本" />

            <StackPanel
                x:Name="GlobalNotificationHost"
                Margin="12,0,12,12"
                MaxWidth="400"
                HorizontalAlignment="Right"
                VerticalAlignment="Bottom"
                Canvas.ZIndex="100"
                Spacing="6">
                <Button
                    x:Name="NotificationOverflowButton"
                    HorizontalAlignment="Right"
                    Click="NotificationOverflowButton_Click"
                    Visibility="Collapsed"
                    AutomationProperties.Name="More notifications" />
                <ItemsControl
                    x:Name="GlobalNotificationStack"
                    ItemsSource="{x:Bind VisibleNotifications, Mode=OneWay}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <StackPanel Spacing="6" />
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <controls:NotificationCard
                                DismissRequested="NotificationCard_DismissRequested"
                                DetailsRequested="NotificationCard_DetailsRequested"
                                IsChinese="{Binding IsChinese}"
                                Notification="{Binding Notification}"
                                PauseRequested="NotificationCard_PauseRequested" />
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>
        </Grid>
    </Grid>
</Window>
