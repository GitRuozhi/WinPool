# WinPool Agent 操作说明

[English](AGENTS.md) | [简体中文（仅供阅读）](AGENTS.zh-CN.md)

> 本文件仅为中文阅读副本；所有操作约束以无 `.zh-CN` 后缀的
> [AGENTS.md](AGENTS.md) 为准。

本文件是 `Program\WinPool` 内的 AI 工作入口和最高优先级操作规则。它说明不能越过什么边界。产品方向、架构、质量门、结果和历史属于 `docs`，只在当前任务需要时读取。

## 优先级

规则冲突时按以下顺序处理：

1. 用户在当前任务中的明确决定。
2. 父级和本地 `AGENTS.md` 中的安全、授权与受保护数据规则。
3. 因任务需要而读取的、对应事实的 `docs` 所有者文档。
4. 当前实现。

通用父级规则不得暗中替换用户更具体的决定。用户已经明确决定：WinPool 历史文档进入 `docs/Archive`；父项目的 `Old` 规则仍适用于非文档的 WinPool 过期内容。

## 默认读取

普通任务开始时，唯一强制读取的项目文档是本文件对应的权威文件 `AGENTS.md`。若当前工作树存在更具体的局部 Agent 规则，同时遵守该局部规则。

默认不读取 README、Product、Development、Quality、CHANGELOG、Reference 或 Archive，也不因存在中英文两份就双份加载。修改前检查 Git 状态、分支、上游与受保护路径。

仅在任务需要时读取其他文档：

| 需要 | 读取 |
| --- | --- |
| 如何运行、当前版本、当前能力、用户可见限制 | `README.md` |
| 产品目标、边界、路线、需求是否在范围内 | `docs/Product.md` |
| 架构、模块所有权、持久化、IPC、staging、版本体系 | `docs/Development.md` |
| 测试、验收词汇、是否适用某类质量门 | `docs/Quality.md` |
| 执行、继续、检查、验收或修改当前正式阶段 | `docs/Plan.md` |
| 已确认的最终结果或重要兼容性变化 | `docs/CHANGELOG.md` |
| 明确的历史设计、版本或被替代决策 | 对应的 `docs/Archive` 文件 |
| 明确的方法、研究或延期观察 | 对应的 `docs/Reference` 文件 |

如何运行和当前能力查 `README.md`；长期产品规则查 `docs/Product.md`；架构查 `docs/Development.md`；测试或正式验收查 `docs/Quality.md`；已完成结果查 `docs/CHANGELOG.md`。项目进度不得只凭 Git 状态回答；问进度时读取 README、Product 和 CHANGELOG。

只有开发者明确要求执行、继续、检查、验收或修改当前正式计划时，才读取 `docs/Plan.md`。文件存在不是读取理由。Archive 和 Reference 永远不是默认上下文，不得批量读取。Reference 不构成当前项目要求。

用户对当前产品、视觉和开发方向的明确决定，高于 Plan、CHANGELOG、Archive 和基于旧约定编写的测试断言。文档与当前决定或已确认实现冲突时，先把文档视为可能过期，不得用旧文档或旧测试反向纠正当前实现。`docs/Plan.md` 只记录用户已确认的当前计划，不是永久产品契约；当前没有活动计划时不得自行从历史文档推导新阶段。

## 环境和范围

- 支持 Windows 和 PowerShell 开发环境。
- 解决方案目标为 .NET 10、WinUI 3、Windows App SDK 和无打包 x64。
- 除非任务明确指出其他路径，只处理 `Program\WinPool`。
- 不重组 Dite、KS、Research、Tests、Showcase 或其他项目。
- 保留无关和用户已有的修改。

## 安全与数据边界

真实存储结构修改默认拒绝。除非当前产品边界或已确认 Plan 明确允许该路径，并且已针对该准确操作和准确目标取得所要求的显式授权，否则不得创建、删除、初始化、格式化、调整、修复或以其他方式修改真实磁盘、分区、卷、存储池、存储层或虚拟磁盘。自由形式存储命令仍然禁止。

UAC 提权或选择 Real 模式不构成授权。模拟仍是存储结构变化的默认路径；即使界面处于 Real 模式，在用户完成真实修改授权流程前也必须保持模拟。

开发 Agent 每次执行实际存储修改前，必须向开发者请求针对该操作及其目标的明确批准。先前的、宽泛的或隐含的批准均不足够。产品中，用户在当前会话显式选择“本机真实修改”选项，即授权执行当前产品边界所允许的受控真实操作；该选项不得预选，也不得把同意持久化。

哪个产品阶段可以增加类型化真实修改路径，由 [产品方向](docs/Product.md) 和已确认 Plan 定义，不由本文件定义。

- 文件测试只能操作明确选择的测试目录中、登记到本次运行的文件；禁止裸设备写入。
- 缓存清理、卷刷新、TRIM/Optimize、进程调度和临时电源计划等辅助动作需要类型化计划、目标校验、必要确认、审计和可逆状态恢复。
- DiskSpd、fio、Dite、RoboCopy、RAMMap 继续作为单独安装的外部工具，由类型化适配器调用；不得捆绑或重新实现。
- 持久化、导出、导入、记录或复制的硬件数据必须经过批准的脱敏边界。
- 不保存或发布独立采集 `.ps1`；固定只读 PowerShell 保持程序集内嵌并通过标准输入传递。

## 文件和文档

- 默认不直接删除文件。狭义例外要求用户批准的重构，并且已确认 Plan 明确列出准确的过期源码或测试目标，且要求先完成替代实现与回归证据。
- WinPool 历史文档进入 `docs/Archive`，并记录真实状态和索引。Archive 内容不是当前要求。
- 其他确认过期的 WinPool 内容进入父项目 `Old`，尽量保留相对路径。
- 低价值生成内容进入父项目 `Rubbish`。
- 不在 WinPool 内创建本地 `Old`、`Rubbish` 或变体目录。
- `README.md` 是面向用户的入口。本文件只保存操作约束。Product、Development、Quality、Plan、CHANGELOG、Reference 和 Archive 内容归 `docs` 管理。
- 同一时间最多只有一个活动 `docs/Plan.md`；无活动阶段时可不存在。已完成或已失效计划冻结在 `docs/Archive`，不得把历史改写成从未出错。
- 同一长期事实只有一个所有者。其他文档最多保留必要摘要和链接，不得复制完整规则。
- Git 保存过程。长期文档保存规则和重要最终结果，不保存 commit 列表、review 流水、每轮测试数量或中间施工记录。
- `assets` 保存软件引用资源并纳入 Git。`OriginArtWork` 保持 Git 忽略，直到用户批准资源方案。
- 无后缀 Markdown 是权威文件；匹配的 `.zh-CN.md` 仅为中文阅读副本。两者不一致时，以无后缀文档为准。权威文件变化时，同一工作项内更新阅读副本。普通任务只读取权威文件。

## Agent 工作流程与 Git

- 只实现用户已确认范围内的最小闭环；不以“后面可能需要”为理由提前增加框架、数据模式、API、任务系统或部署设施。
- 改变产品行为、数据边界或阶段范围前必须获得确认；用户已经明确决定的事项不重复交由旧文档裁决。
- 不把讨论稿和未确认方案写入长期文档；不创建额外规划文档，除非开发者明确要求。
- 文档、重构、功能、视觉或资源修改分开提交；等价重构与视觉调整分开提交。
- 本仓库为单人开发项目，所有提交直接落在 `main` 分支；不创建功能分支、不发起 Pull Request。
- 每次完成范围内的修改后默认创建本地 Git 提交，无需询问；遵循仓库提交习惯。不提交生成物 `artifacts/` 或无关的用户文件。
- 默认不推送远端。未推送提交累计达到 5 个时，主动提醒开发者推送远端。
- 推送前必须 fetch，确认 `origin/main` 是本地 HEAD 的祖先，并检查待推提交；拒绝分叉和 force push。
- 除非开发者明确授权该动作，否则不得打 tag、创建 GitHub Release、上传二进制或部署。

项目版本定义在 `Directory.Build.props`。不得发明第二套版本体系。架构与路线图通常只写到 `Va.b`。迭代号 `c=8` 或 `c=9` 时提醒开发者控制范围；不得创建 `c=10`。

## 验证触发

任务涉及测试或验收时，使用 [质量规则](docs/Quality.md) 中的结果词汇和门定义。

- 纯文档修改不执行代码、native、设备或视觉测试。
- 普通小修改和普通功能开发不执行完整质量门。
- 开发者指定某项测试时，执行该范围。
- 若修改存在明显局部风险，可以执行与当前修改直接相关的最小检查，不得自动升级为完整验证。
- 正式阶段实现完成不自动开始完整验收。应询问开发者是否进入正式测试。
- 未运行或不可用的门不得报告为通过。
- 自动检查不能替代 UAC、托盘、原生选择器、视觉、设备或长时间人工证据。
- 真实硬件结构修改不是可接受的验证方法，除非已确认 Plan 在允许真实修改的阶段要求该项验证，并遵守上述授权规则。
